![30DaysOfPython](./images/30DaysOfPython_banner3@2x.png)

🧳 [ตอนที่ 1: วันที่ 1 - 3](day1-3.md)  
🧳 [ตอนที่ 2: วันที่ 4 - 6](day4-6.md)  
🧳 [ตอนที่ 3: วันที่ 7 - 9](day7-9.md)  
🧳 [ตอนที่ 4: วันที่ 10 - 12](day10-12.md)  
🧳 [ตอนที่ 5: วันที่ 13 - 15](day13-15.md)  
🧳 [ตอนที่ 6: วันที่ 16 - 18](day16-18.md)  
🧳 [ตอนที่ 7: วันที่ 19 - 21](day19-21.md)  
🧳 [ตอนที่ 8: วันที่ 22 - 24](day22-24.md)  
🧳 [ตอนที่ 9: วันที่ 25 - 27](day25-27.md)  
🧳 [ตอนที่ 10: วันที่ 28 - 30](day28-30.md)  
- [📘 วันที่ 28](#%f0%9f%93%98-day-28)
- [Application Programming Interface (API)](#application-programming-interfaceapi)
  - [API](#api)
  - [การสร้าง API](#building-api)
  - [HTTP (Hypertext Transfer Protocol)](#httphypertext-transfer-protocol)
  - [โครงสร้างของ HTTP](#structure-of-http)
  - [บรรทัด Request เริ่มต้น (Status Line)](#initial-request-linestatus-line)
    - [บรรทัด Response เริ่มต้น (Status Line)](#initial-response-linestatus-line)
    - [Header Fields](#header-fields)
    - [เนื้อหาของข้อความ (message body)](#the-message-body)
    - [Request Methods](#request-methods)
  - [💻 แบบฝึกหัด: วันที่ 28](#%f0%9f%92%bb-exercises-day-28)
  - [วันที่ 29](#day-29)
  - [การสร้าง API](#building-api-1)
    - [โครงสร้างของ API](#structure-of-an-api)
    - [การดึงข้อมูลโดยใช้ get](#retrieving-data-using-get)
    - [การรับเอกสารด้วย id](#getting-a-document-by-id)
    - [การสร้างข้อมูลโดยใช้ POST](#creating-data-using-post)
    - [การอัปเดตโดยใช้ PUT](#updating-using-put)
    - [การลบเอกสารโดยใช้ Delete](#deleting-a-document-using-delete)
  - [💻 แบบฝึกหัด: วันที่ 29](#%f0%9f%92%bb-exercises-day-29)
- [วันที่ 30](#day-30)
  - [บทสรุป](#conclusions)

# 📘 วันที่ 28
# Application Programming Interface (API)
## API
Application Programming Interface (API) ชนิดของ API ที่เราจะพูดถึงในหัวข้อนี้คือ Web API
Web API คืออินเทอร์เฟซที่กำหนดไว้ซึ่งใช้สำหรับการโต้ตอบระหว่างองค์กรและแอปพลิเคชันที่ใช้ทรัพยากรขององค์กรนั้น ซึ่งก็คือข้อตกลงระดับบริการ (Service Level Agreement หรือ SLA) เพื่อระบุผู้ให้บริการเชิงฟังก์ชันและเปิดเผยเส้นทางบริการหรือ URL สำหรับผู้ใช้ API นั้น
ในบริบทของการพัฒนาเว็บ API ถูกนิยามว่าเป็นชุดของข้อกำหนด เช่น ข้อความ request แบบ Hypertext Transfer Protocol (HTTP) พร้อมกับการนิยามโครงสร้างของข้อความ response ซึ่งโดยปกติจะอยู่ในรูปแบบ XML หรือ JavaScript Object Notation (JSON)
Web API ได้เปลี่ยนแปลงออกจาก Simple Object Access Protocol (SOAP) แบบ web service และสถาปัตยกรรมเชิงบริการ (SOA) ไปสู่รูปแบบ web resource แบบ representational state transfer (REST) ที่ตรงไปตรงมามากขึ้น
บริการโซเชียลมีเดีย web API ทำให้ชุมชนบนเว็บสามารถแชร์เนื้อหาและข้อมูลระหว่างชุมชนและแพลตฟอร์มต่าง ๆ ได้ การใช้ API เนื้อหาที่ถูกสร้างขึ้นในที่หนึ่งสามารถถูกโพสต์และอัปเดตไปยังหลายตำแหน่งบนเว็บได้แบบไดนามิก
ตัวอย่างเช่น REST API ของ Twitter ช่วยให้นักพัฒนาสามารถเข้าถึงข้อมูลหลักของ Twitter ได้ และ Search API มีเมธอดสำหรับนักพัฒนาในการโต้ตอบกับข้อมูล Twitter Search และ trends แอปพลิเคชันจำนวนมากมี API endpoint ตัวอย่าง [API](https://restcountries.eu/rest/v2/all)
ในหัวข้อนี้ เราจะพูดถึง RESTful API ที่ใช้ HTTP request method GET, PUT, POST และ DELETE ในการจัดการข้อมูล

## การสร้าง API
RESTful API คืออินเทอร์เฟซโปรแกรมประยุกต์ (API) ที่ใช้ HTTP request ในการ GET, PUT, POST และ DELETE ข้อมูล ในหัวข้อก่อนหน้า เราได้เรียนรู้เกี่ยวกับ python, flask และ mongoDB มาแล้ว เราจะใช้ความรู้ที่เราได้รับเพื่อพัฒนา RESTful API โดยใช้ python flask และ mongoDB ทุกแอปพลิเคชันที่มีการดำเนินการ CRUD (Create, Read, Update, Delete) จะมี API สำหรับสร้างข้อมูล รับข้อมูล อัปเดตข้อมูล หรือลบข้อมูลจากฐานข้อมูล

หากต้องการสร้าง API มันจะดีถ้าเราเข้าใจ HTTP protocol
## HTTP (Hypertext Transfer Protocol)
HTTP คือ protocol การสื่อสารที่กำหนดไว้ระหว่างไคลเอนต์และเซิร์ฟเวอร์ ไคลเอนต์ในกรณีนี้คือเบราว์เซอร์ และเซิร์ฟเวอร์คือสถานที่ที่คุณเข้าถึงข้อมูล HTTP คือ network protocol ที่ใช้ในการส่งมอบทรัพยากร ซึ่งอาจเป็นไฟล์บน World Wide Web ไม่ว่าจะเป็นไฟล์ HTML ไฟล์รูปภาพ ผลลัพธ์ query สคริปต์ หรือชนิดไฟล์อื่น ๆ

เบราว์เซอร์คือ HTTP client เพราะมันส่ง request ไปยัง HTTP server (Web server) ซึ่งจากนั้นจะส่ง response กลับมายังไคลเอนต์

## โครงสร้างของ HTTP 
HTTP ใช้โมเดล client-server HTTP client เปิดการเชื่อมต่อและส่งข้อความ request ไปยัง HTTP server และ HTTP server จะคืนข้อความซึ่งก็คือทรัพยากรที่ถูกร้องขอ เมื่อวงจร request response เสร็จสิ้นแล้ว เซิร์ฟเวอร์จะปิดการเชื่อมต่อ

![HTTP request response cycle](./images/http_request_response_cycle.png)


รูปแบบของข้อความ request และ response มีลักษณะคล้ายกัน ข้อความทั้งสองประเภทมี
* บรรทัดเริ่มต้น
* บรรทัด header ศูนย์บรรทัดหรือมากกว่า
* บรรทัดว่าง (คือ CRLF เพียงอย่างเดียว) และ
* เนื้อหาของข้อความที่เป็นทางเลือก (เช่น ไฟล์ หรือข้อมูล query หรือผลลัพธ์ query)

มาดูตัวอย่างข้อความ request และ response โดยการเข้าไปที่เว็บไซต์นี้: https://thirtydaysofpython-v1-final.herokuapp.com/

![Request and Response header](./images/request_response_header.png)

## บรรทัด Request เริ่มต้น (Status Line)
บรรทัด request เริ่มต้นแตกต่างจาก response
บรรทัด request มีสามส่วน คั่นด้วยช่องว่าง: 
* ชื่อเมธอด (GET, POST, HEAD)
* เส้นทางของทรัพยากรที่ถูกร้องขอ 
* เวอร์ชันของ HTTP ที่ใช้ เช่น GET / HTTP/1.1
* 
GET เป็น HTTP ที่พบบ่อยที่สุดในการรับหรืออ่านทรัพยากร และ POST เป็น request method ทั่วไปในการสร้างทรัพยากร

### บรรทัด Response เริ่มต้น (Status Line)
บรรทัด response เริ่มต้น เรียกว่า status line ก็มีสามส่วนคั่นด้วยช่องว่างเช่นกัน: 
* เวอร์ชันของ HTTP
* Response status code ที่ให้ผลลัพธ์ของ request และเหตุผลที่อธิบาย status code ตัวอย่างของ status line ได้แก่:
HTTP/1.0 200 OK
หรือ
HTTP/1.0 404 Not Found
หมายเหตุ:

status code ที่พบบ่อยที่สุดได้แก่:
200 OK: request สำเร็จ และทรัพยากรที่เป็นผลลัพธ์ (เช่น ไฟล์หรือผลลัพธ์สคริปต์) ถูกคืนกลับมาในเนื้อหาของข้อความ
500 Server Error
รายการ HTTP status code แบบครบถ้วนสามารถดูได้ที่[นี่](https://httpstatuses.com/)

### Header Fields
ดังที่คุณเห็นในภาพหน้าจอด้านบน บรรทัด header ให้ข้อมูลเกี่ยวกับ request หรือ response หรือเกี่ยวกับอ็อบเจ็กต์ที่ถูกส่งในเนื้อหาของข้อความ
```sh
GET / HTTP/1.1
Host: thirtydaysofpython-v1-final.herokuapp.com
Connection: keep-alive
Pragma: no-cache
Cache-Control: no-cache
Upgrade-Insecure-Requests: 1
User-Agent: Mozilla/5.0 (Macintosh; Intel Mac OS X 10_14_6) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/79.0.3945.79 Safari/537.36
Sec-Fetch-User: ?1
Accept: text/html,application/xhtml+xml,application/xml;q=0.9,image/webp,image/apng,*/*;q=0.8,application/signed-exchange;v=b3;q=0.9
Sec-Fetch-Site: same-origin
Sec-Fetch-Mode: navigate
Referer: https://thirtydaysofpython-v1-final.herokuapp.com/post
Accept-Encoding: gzip, deflate, br
Accept-Language: en-GB,en;q=0.9,fi-FI;q=0.8,fi;q=0.7,en-CA;q=0.6,en-US;q=0.5,fr;q=0.4
```
### เนื้อหาของข้อความ (message body)
ข้อความ HTTP อาจมีเนื้อหาข้อมูลที่ถูกส่งหลังจากบรรทัด header ใน response นี่คือจุดที่ทรัพยากรที่ถูกร้องขอถูกส่งกลับไปยังไคลเอนต์ (การใช้งานที่พบบ่อยที่สุดของเนื้อหาข้อความ) หรืออาจเป็นข้อความอธิบายถ้ามี error ใน request นี่คือจุดที่ข้อมูลที่ผู้ใช้ป้อนหรือไฟล์ที่อัปโหลดถูกส่งไปยังเซิร์ฟเวอร์

ถ้าข้อความ HTTP มีเนื้อหา โดยปกติจะมีบรรทัด header ในข้อความที่อธิบายเนื้อหานั้น โดยเฉพาะอย่างยิ่ง

header Content-Type: ให้ MIME-type ของข้อมูลในเนื้อหา (text/html, application/json, text/plain, text/css, image/gif)
header Content-Length: ให้จำนวนไบต์ในเนื้อหา

### Request Methods
GET, POST, PUT และ DELETE คือ HTTP request method ที่เราจะนำไปใช้สร้าง API หรือแอปพลิเคชันที่มีการดำเนินการ CRUD
1. GET: เมธอด GET ใช้ในการดึงและรับข้อมูลจากเซิร์ฟเวอร์ที่กำหนดโดยใช้ URI ที่กำหนด request ที่ใช้ GET ควรดึงข้อมูลเท่านั้น และไม่ควรมีผลกระทบอื่นใดต่อข้อมูล

2. POST: request POST ใช้เพื่อสร้างข้อมูลและส่งข้อมูลไปยังเซิร์ฟเวอร์ เช่น การสร้างโพสต์ใหม่ การอัปโหลดไฟล์ ฯลฯ โดยใช้ HTML form

3. PUT: แทนที่การแสดงผลปัจจุบันทั้งหมดของทรัพยากรเป้าหมายด้วยเนื้อหาที่อัปโหลด และเราใช้มันเพื่อแก้ไขหรืออัปเดตข้อมูล

4. DELETE: ลบข้อมูล


## 💻 แบบฝึกหัด: วันที่ 28
1. อ่านเกี่ยวกับ API และ HTTP

##  วันที่ 29

## การสร้าง API

ตัวอย่าง [API](https://restcountries.eu/rest/v2/all)
ในหัวข้อนี้ เราจะพูดถึง RESTful API ที่ใช้ HTTP request method GET, PUT, POST และ DELETE ในการจัดการข้อมูล

RESTful API คืออินเทอร์เฟซโปรแกรมประยุกต์ (API) ที่ใช้ HTTP request ในการ GET, PUT, POST และ DELETE ข้อมูล ในหัวข้อก่อนหน้า เราได้เรียนรู้เกี่ยวกับ python, flask และ mongoDB มาแล้ว เราจะใช้ความรู้ที่เราได้รับเพื่อพัฒนา RESTful API โดยใช้ python flask และ mongoDB ทุกแอปพลิเคชันที่มีการดำเนินการ CRUD (Create, Read, Update, Delete) จะมี API สำหรับสร้างข้อมูล รับข้อมูล อัปเดตข้อมูล หรือลบข้อมูลจากฐานข้อมูล

เบราว์เซอร์สามารถจัดการได้เฉพาะ get request เท่านั้น ดังนั้น เราต้องมีเครื่องมือที่สามารถช่วยให้เราจัดการ request method ทั้งหมดได้ (GET, POST, PUT, DELETE) [Postman](https://www.getpostman.com/) เป็นเครื่องมือที่ได้รับความนิยมมากเมื่อพูดถึงการพัฒนา API ดังนั้น ถ้าคุณต้องการทำหัวข้อนี้ คุณต้อง[ดาวน์โหลด postman](https://www.getpostman.com/)
![Postman](./images/postman.png)

### โครงสร้างของ API
API endpoint คือ URL ที่ช่วยในการดึง สร้าง อัปเดต หรือลบทรัพยากร โครงสร้างมีลักษณะดังนี้:
ตัวอย่าง:
https://api.twitter.com/1.1/lists/members.json
คืนค่าสมาชิกของลิสต์ที่ระบุ สมาชิกของลิสต์ส่วนตัวจะแสดงก็ต่อเมื่อผู้ใช้ที่ผ่านการยืนยันตัวตนเป็นเจ้าของลิสต์ที่ระบุเท่านั้น
ชื่อของบริษัทตามด้วยเวอร์ชันตามด้วยจุดประสงค์ของ API
เมธอด:
HTTP methods & URLs

API ใช้ HTTP method ต่อไปนี้สำหรับการจัดการอ็อบเจ็กต์:
```sh
GET        Used for object retrieval
POST       Used for object creation and object actions
PUT        Used for object update
DELETE     Used for object deletion
```
มาสร้าง api ที่รวบรวมข้อมูลเกี่ยวกับนักเรียน 30DaysOfPython กัน เราจะรวบรวมชื่อ ประเทศ เมือง วันเกิด ทักษะ และประวัติย่อ

หากต้องการสร้าง API นี้ เราจะใช้:
* Postman
* Python
* Flask
* MongoDB

### การดึงข้อมูลโดยใช้ get

ในขั้นตอนนี้ มาใช้ข้อมูลตัวอย่างและคืนค่ามันเป็น json กัน หากต้องการคืนค่าเป็น json เราจะใช้โมดูล json และโมดูล Response
```py
# let's import the flask

from flask import Flask,  Response
import json

app = Flask(__name__)

@app.route('/api/v1.0/students', methods = ['GET']) 
def students ():
    student_list = [
        {
            'name':'Asabeneh',
            'country':'Finland',
            'city':'Helsinki',
            'skills':['HTML', 'CSS','JavaScript','Python']
        },
        {
            'name':'David',
            'country':'UK',
            'city':'London',
            'skills':['Python','MongoDB']
        },
        {
            'name':'John',
            'country':'Sweden',
            'city':'Stockholm',
            'skills':['Java','C#']
        }
    ]
    return Response(json.dumps(student_list), mimetype='application/json')

   
if __name__ == '__main__':
    # for deployment
    # to make it work for both production and development
    port = int(os.environ.get("PORT", 5000))
    app.run(debug=True, host='0.0.0.0', port=port)
```
เมื่อคุณร้องขอ url http://localhost:5000/api/v1.0/students บนเบราว์เซอร์ คุณจะได้สิ่งนี้:

![Get on browser](./images/get_on_browser.png)

เมื่อคุณร้องขอ url http://localhost:5000/api/v1.0/students บนเบราว์เซอร์ คุณจะได้สิ่งนี้:


![Get on postman](./images/get_on_postman.png)

แทนที่จะแสดงข้อมูลตัวอย่าง มาเชื่อมต่อแอปพลิเคชัน flask กับ MongoDB และรับข้อมูลจากฐานข้อมูล mongoDB กัน

```py
# let's import the flask

from flask import Flask,  Response
import json
import pymongo


app = Flask(__name__)

# 
MONGODB_URI='mongodb+srv://asabeneh:your_password@30daysofpython-twxkr.mongodb.net/test?retryWrites=true&w=majority'
client = pymongo.MongoClient(MONGODB_URI)
db = client['thirty_days_of_python'] # accessing the database

@app.route('/api/v1.0/students', methods = ['GET'])
def students ():
    
    return Response(json.dumps(student), mimetype='application/json')

   
if __name__ == '__main__':
    # for deployment
    # to make it work for both production and development
    port = int(os.environ.get("PORT", 5000))
    app.run(debug=True, host='0.0.0.0', port=port)
```
โดยการเชื่อมต่อ flask เราสามารถดึงข้อมูล collection students จากฐานข้อมูล thirty_days_of_python ได้
```sh
[
    {
        "_id": {
            "$oid": "5df68a21f106fe2d315bbc8b"
        },
        "name": "Asabeneh",
        "country": "Finland",
        "city": "Helsinki",
        "age": 38
    },
    {
        "_id": {
            "$oid": "5df68a23f106fe2d315bbc8c"
        },
        "name": "David",
        "country": "UK",
        "city": "London",
        "age": 34
    },
    {
        "_id": {
            "$oid": "5df68a23f106fe2d315bbc8e"
        },
        "name": "Sami",
        "country": "Finland",
        "city": "Helsinki",
        "age": 25
    }
]
```
### การรับเอกสารด้วย id
เราสามารถเข้าถึงเอกสารเดี่ยวได้โดยใช้ id มาเข้าถึง Asabeneh โดยใช้ id ของเขากัน
http://localhost:5000/api/v1.0/students/5df68a21f106fe2d315bbc8b

```py
# let's import the flask

from flask import Flask,  Response
import json
from bson.objectid import ObjectId
import json
from bson.json_util import dumps
import pymongo


app = Flask(__name__)

# 
MONGODB_URI='mongodb+srv://asabeneh:your_password@30daysofpython-twxkr.mongodb.net/test?retryWrites=true&w=majority'
client = pymongo.MongoClient(MONGODB_URI)
db = client['thirty_days_of_python'] # accessing the database

@app.route('/api/v1.0/students', methods = ['GET']) 
def students ():
    
    return Response(json.dumps(student), mimetype='application/json')
@app.route('/api/v1.0/students/<id>', methods = ['GET']) 
def single_student (id):
    student = db.students.find({'_id':ObjectId(id)})
    return Response(dumps(student), mimetype='application/json')
   
if __name__ == '__main__':
    # for deployment
    # to make it work for both production and development
    port = int(os.environ.get("PORT", 5000))
    app.run(debug=True, host='0.0.0.0', port=port)
```

```sh
[
    {
        "_id": {
            "$oid": "5df68a21f106fe2d315bbc8b"
        },
        "name": "Asabeneh",
        "country": "Finland",
        "city": "Helsinki",
        "age": 38
    }
]
```
### การสร้างข้อมูลโดยใช้ POST
เราใช้ POST request method เพื่อสร้างข้อมูล
```py
# let's import the flask

from flask import Flask,  Response
import json
from bson.objectid import ObjectId
import json
from bson.json_util import dumps
import pymongo
from datetime import datetime


app = Flask(__name__)

# 
MONGODB_URI='mongodb+srv://asabeneh:your_password@30daysofpython-twxkr.mongodb.net/test?retryWrites=true&w=majority'
client = pymongo.MongoClient(MONGODB_URI)
db = client['thirty_days_of_python'] # accessing the database

@app.route('/api/v1.0/students', methods = ['GET']) 
def students ():
    
    return Response(json.dumps(student), mimetype='application/json')
@app.route('/api/v1.0/students/<id>', methods = ['GET']) 
def single_student (id):
    student = db.students.find({'_id':ObjectId(id)})
    return Response(dumps(student), mimetype='application/json')
@app.route('/api/v1.0/students', methods = ['POST']) 
def create_student ():
    name = request.form['name']
    country = request.form['country']
    city = request.form['city']
    skills = request.form['skills'].split(', ')
    bio = request.form['bio']
    birthyear = request.form['birthyear']
    created_at = datetime.now()
    student = {
        'name': name,
        'country': country,
        'city': city,
        'birthyear': birthyear,
        'skills': skills,
        'bio': bio,
        'created_at': created_at

    }
    db.students.insert_one(student)
    return ;
def update_student (id):
if __name__ == '__main__':
    # for deployment
    # to make it work for both production and development
    port = int(os.environ.get("PORT", 5000))
    app.run(debug=True, host='0.0.0.0', port=port)
```
### การอัปเดตโดยใช้ PUT
```py
# let's import the flask

from flask import Flask,  Response
import json
from bson.objectid import ObjectId
import json
from bson.json_util import dumps
import pymongo
from datetime import datetime


app = Flask(__name__)

# 
MONGODB_URI='mongodb+srv://asabeneh:your_password@30daysofpython-twxkr.mongodb.net/test?retryWrites=true&w=majority'
client = pymongo.MongoClient(MONGODB_URI)
db = client['thirty_days_of_python'] # accessing the database

@app.route('/api/v1.0/students', methods = ['GET']) 
def students ():
    
    return Response(json.dumps(student), mimetype='application/json')
@app.route('/api/v1.0/students/<id>', methods = ['GET']) 
def single_student (id):
    student = db.students.find({'_id':ObjectId(id)})
    return Response(dumps(student), mimetype='application/json')
@app.route('/api/v1.0/students', methods = ['POST']) 
def create_student ():
    name = request.form['name']
    country = request.form['country']
    city = request.form['city']
    skills = request.form['skills'].split(', ')
    bio = request.form['bio']
    birthyear = request.form['birthyear']
    created_at = datetime.now()
    student = {
        'name': name,
        'country': country,
        'city': city,
        'birthyear': birthyear,
        'skills': skills,
        'bio': bio,
        'created_at': created_at

    }
    db.students.insert_one(student)
    return 
@app.route('/api/v1.0/students/<id>', methods = ['PUT']) # this decorator create the home route
def update_student (id):
    query = {"_id":ObjectId(id)}
    name = request.form['name']
    country = request.form['country']
    city = request.form['city']
    skills = request.form['skills'].split(', ')
    bio = request.form['bio']
    birthyear = request.form['birthyear']
    created_at = datetime.now()
    student = {
        'name': name,
        'country': country,
        'city': city,
        'birthyear': birthyear,
        'skills': skills,
        'bio': bio,
        'created_at': created_at

    }
    db.students.update_one(query, student)
    # return Response(dumps({"result":"a new student has been created"}), mimetype='application/json')
    return 
def update_student (id):
if __name__ == '__main__':
    # for deployment
    # to make it work for both production and development
    port = int(os.environ.get("PORT", 5000))
    app.run(debug=True, host='0.0.0.0', port=port)
```

### การลบเอกสารโดยใช้ Delete
```py
# let's import the flask

from flask import Flask,  Response
import json
from bson.objectid import ObjectId
import json
from bson.json_util import dumps
import pymongo
from datetime import datetime


app = Flask(__name__)

# 
MONGODB_URI='mongodb+srv://asabeneh:your_password@30daysofpython-twxkr.mongodb.net/test?retryWrites=true&w=majority'
client = pymongo.MongoClient(MONGODB_URI)
db = client['thirty_days_of_python'] # accessing the database

@app.route('/api/v1.0/students', methods = ['GET']) 
def students ():
    
    return Response(json.dumps(student), mimetype='application/json')
@app.route('/api/v1.0/students/<id>', methods = ['GET']) 
def single_student (id):
    student = db.students.find({'_id':ObjectId(id)})
    return Response(dumps(student), mimetype='application/json')
@app.route('/api/v1.0/students', methods = ['POST']) 
def create_student ():
    name = request.form['name']
    country = request.form['country']
    city = request.form['city']
    skills = request.form['skills'].split(', ')
    bio = request.form['bio']
    birthyear = request.form['birthyear']
    created_at = datetime.now()
    student = {
        'name': name,
        'country': country,
        'city': city,
        'birthyear': birthyear,
        'skills': skills,
        'bio': bio,
        'created_at': created_at

    }
    db.students.insert_one(student)
    return 
@app.route('/api/v1.0/students/<id>', methods = ['PUT']) # this decorator create the home route
def update_student (id):
    query = {"_id":ObjectId(id)}
    name = request.form['name']
    country = request.form['country']
    city = request.form['city']
    skills = request.form['skills'].split(', ')
    bio = request.form['bio']
    birthyear = request.form['birthyear']
    created_at = datetime.now()
    student = {
        'name': name,
        'country': country,
        'city': city,
        'birthyear': birthyear,
        'skills': skills,
        'bio': bio,
        'created_at': created_at

    }
    db.students.update_one(query, student)
    # return Response(dumps({"result":"a new student has been created"}), mimetype='application/json')
    return 
@app.route('/api/v1.0/students/<id>', methods = ['PUT']) # this decorator create the home route
def update_student (id):
    query = {"_id":ObjectId(id)}
    name = request.form['name']
    country = request.form['country']
    city = request.form['city']
    skills = request.form['skills'].split(', ')
    bio = request.form['bio']
    birthyear = request.form['birthyear']
    created_at = datetime.now()
    student = {
        'name': name,
        'country': country,
        'city': city,
        'birthyear': birthyear,
        'skills': skills,
        'bio': bio,
        'created_at': created_at

    }
    db.students.update_one(query, student)
    # return Response(dumps({"result":"a new student has been created"}), mimetype='application/json')
    return ;
@app.route('/api/v1.0/students/<id>', methods = ['DELETE']) 
def delete_student (id):
    db.students.delete_one({"_id":ObjectId(id)})
    return
if __name__ == '__main__':
    # for deployment
    # to make it work for both production and development
    port = int(os.environ.get("PORT", 5000))
    app.run(debug=True, host='0.0.0.0', port=port)
```

## 💻 แบบฝึกหัด: วันที่ 29
1. นำตัวอย่างข้างต้นไปใช้และพัฒนา[สิ่งนี้](https://thirtydayofpython-api.herokuapp.com/)

# วันที่ 30
## บทสรุป
ในกระบวนการเตรียมเนื้อหานี้ ผมได้เรียนรู้มากมาย และคุณได้เป็นแรงบันดาลใจให้ผมทำมากขึ้น ขอแสดงความยินดีที่คุณมาถึงระดับนี้ ถ้าคุณทำแบบฝึกหัดและโปรเจกต์ทั้งหมดแล้ว ตอนนี้คุณมีความสามารถที่จะไปในเส้นทาง data analysis, data science, machine learning หรือ web development ได้แล้ว

GIVE FEEDBACK: http://thirtydayofpython-api.herokuapp.com/feedback



[<< ตอนที่ 9](day25-25.md) | [ตอนที่ 1 >>](day1-3.md)



